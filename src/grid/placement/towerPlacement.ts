import { TOWER_MARKER } from "../../commons/constants";
import { PlacementManager } from "./placementManager";

export class TowerPlacement extends PlacementManager {
    public MARKER: string = TOWER_MARKER
}